# Auto detect text files and perform LF normalization
* text=auto

# Go source files should always use LF
*.go text eol=lf

# Go mod files
go.mod text eol=lf
go.sum text eol=lf

# Shell scripts
*.sh text eol=lf

# PowerShell scripts (can use CRLF on Windows)
*.ps1 text eol=crlf

# YAML files (GitHub Actions)
*.yml text eol=lf
*.yaml text eol=lf

# Markdown
*.md text eol=lf

# JSON
*.json text eol=lf
